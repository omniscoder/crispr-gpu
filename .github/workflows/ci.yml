name: CI

on:
  push:
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configure
        run: cmake -B build -S . -DCRISPR_GPU_ENABLE_CUDA=OFF -DCRISPR_GPU_BUILD_PYTHON=OFF -DCMAKE_BUILD_TYPE=Release
      - name: Build
        run: cmake --build build --config Release
      - name: Tests
        run: ctest --output-on-failure --test-dir build
      - name: Synthetic benchmark (CPU only)
        run: |
          SKIP_GPU=1 CRISPR_GPU_WARMUP=0 BENCH_SCALE=small ./benchmarks/run_synthetic.sh
